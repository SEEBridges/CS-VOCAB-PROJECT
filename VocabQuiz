import java.util.*;
import java.io.*;

/**
 * The VocabQuiz class.
 * 
 * @author (Austin Singer)
 * @version (11/21/22)
 */
public class VocabQuiz
{
    /**
     * Method main.
     *
     * @param args command line arguments
     */
    public static void main(String[] args) throws java.io.IOException,FileNotFoundException
    {
        Scanner kb = new Scanner(System.in);
        GamePlay gameplay = new GamePlay("VocabList.txt");
        
        System.out.print("1 - Print All Entries\n" + "2 - Play Short Round\n"
            + "3 - Play Long Round\n" + "4 - Continuous Play\n" + "5 - Exit" );
        
        int i = kb.nextInt();    
        gameplay.makeSelection(i);    
            
        do
        {
            gameplay.printResults();
        }
        while(i == 1);
        do
        {
            gameplay.playLimitedGame(4);
        }
        while(i == 2);
        do
        {
            gameplay.playRound();
        }
        while(i == 3);
        do
        {
            gameplay.playContinuousGame();
        }
        while(i == 4);
        do
        {
            System.out.println("GoodBye.");
        }
        while(i==5);
        } 
}
